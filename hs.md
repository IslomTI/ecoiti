Короче говоря: Нет, сразу работать не будет.
Есть один критический момент, о котором я предупреждал выше (конфликт тегов VLAN).
В чем проблема?
 * Твой sw1 (по шпаргалке): На порту ens3 (который идет к фаерволу) настроен VLAN 300 (Tagged). Это значит, что свитч отправляет пакеты с "биркой" 300.
 * Твой fw (Ideco): При начальной настройке (когда ты задаешь IP 10.10.30.1 в черном окне) он вешает этот адрес на физический порт без VLAN (Untagged). Он не поймет пакеты с "биркой" и отбросит их.
Как сделать, чтобы заработало (План действий)
Так как admin-cod подключен к свитчу без тегов (это правильно в твоей шпоре), админ увидит свитч, но не увидит фаервол.
Сделай этот трюк на sw1, чтобы настроить фаервол:
 * Временно убери тег на свитче sw1:
   Открой файл портов моста br300:
   nano /etc/net/ifaces/br300/ports

   Поменяй ens3.300 на просто ens3.
   (Теперь свитч будет слать на фаервол чистый трафик без тегов).
   Перезапусти сеть:
   systemctl restart network

 * Зайди на фаервол:
   Теперь с admin-cod (c IP 10.10.30.100) ты сможешь открыть https://10.10.30.1.
 * Настрой VLAN на фаерволе (В веб-интерфейсе):
   * Пройди мастер настройки.
   * В разделе "Сервисы" -> "Интерфейсы" создай VLAN 300 на локальном порту.
   * Перевесь IP 10.10.30.1 с физического порта на этот VLAN 300.
   * (В этот момент связь пропадет, это нормально).
 * Верни тег на свитче sw1 (Как было в шпоре):
   Снова открой nano /etc/net/ifaces/br300/ports.
   Верни ens3 обратно на ens3.300.
   Перезапусти сеть:
   systemctl restart network

Итог: Связь появится, и теперь все будет работать правильно и по заданию.
